__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/
*.log

# Node side (the npm wrapper)
node_modules/
*.tgz

# The scanner's own output — `--sarif` defaults to ./o1js-scan.sarif, so
# running the tool inside its own checkout would otherwise stage a report.
*.sarif

.DS_Store
